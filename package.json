{
  "name": "estuary",
  "version": "0.0.0",
  "scripts": {
    "build": "npx tailwindcss -i styles/tailwind.css -o styles/main.dist.css",
    "watch": "npx tailwindcss -i styles/tailwind.css -o styles/main.dist.css --watch"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "tailwindcss": "^3.0.0"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.0",
    "npm-watch": "^0.11.0"
  }
}
